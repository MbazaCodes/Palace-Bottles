import StaffClient from "@/components/admin/StaffClient";

export const metadata = { title: "Staff & Roles — Palace Bottles Admin" };

export default function AdminStaffPage() {
  return <StaffClient />;
}
